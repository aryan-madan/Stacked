import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aryan.stacked',
  appName: 'Stacked',
  webDir: 'dist',
  backgroundColor: '#0a0a0a',
};

export default config;
